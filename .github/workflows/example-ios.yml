name: Example iOS Build

on:
  workflow_dispatch:

concurrency:
  group: example-ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Build library TypeScript
        run: npm run build

      - name: Install example dependencies
        run: npm ci
        working-directory: examples/bare-new-arch

      - name: Pod install
        working-directory: examples/bare-new-arch/ios
        run: |
          NO_FLIPPER=1 pod install

      - name: Verify Xcode scheme
        working-directory: examples/bare-new-arch/ios
        run: |
          xcodebuild -workspace FlipperKitBareNewArch.xcworkspace -list
          xcodebuild -workspace FlipperKitBareNewArch.xcworkspace -list | grep -q "FlipperKitBareNewArch"

      - name: Build iOS Simulator app
        working-directory: examples/bare-new-arch/ios
        run: |
          xcodebuild \
            -workspace FlipperKitBareNewArch.xcworkspace \
            -scheme FlipperKitBareNewArch \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build
